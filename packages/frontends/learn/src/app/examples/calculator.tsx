import styles from "../styles/examples/calculator.module.scss";

export default function CalculatorPage(): JSX.Element {
  return (
    <section className={styles.root}>
      <h1>Calculator Page</h1>
    </section>
  );
}
