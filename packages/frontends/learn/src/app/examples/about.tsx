import styles from "../styles/examples/about.module.scss";

export default function AboutPage(): JSX.Element {
  return (
    <section className={styles.root}>
      <h1>About Page</h1>
    </section>
  );
}
