import styles from "../styles/examples/shopping.module.scss";

export default function ShoppingPage(): JSX.Element {
  return (
    <section className={styles.root}>
      <h1>Shopping Page</h1>
    </section>
  );
}
