import styles from "../styles/examples/counter.module.scss";

export default function CounterPage(): JSX.Element {
  return (
    <section className={styles.root}>
      <h1>Counter Page</h1>
    </section>
  );
}
