import styles from "../styles/examples/profile.module.scss";

export default function ProfilePage(): JSX.Element {
  return (
    <section className={styles.root}>
      <h1>Profile Page</h1>
    </section>
  );
}
