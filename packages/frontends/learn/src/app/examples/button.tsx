import styles from "../styles/examples/button.module.scss";

export default function ButtonPage(): JSX.Element {
  return (
    <section className={styles.root}>
      <h1>Button Page</h1>
    </section>
  );
}
