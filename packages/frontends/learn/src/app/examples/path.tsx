import styles from "../styles/examples/path.module.scss";

export default function PathPage(): JSX.Element {
  return (
    <section className={styles.root}>
      <h1>Path Page</h1>
    </section>
  );
}
